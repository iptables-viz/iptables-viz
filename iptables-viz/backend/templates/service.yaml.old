apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name }}
spec:
  selector:
    app: iptables-viz-backend
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8080
